{
  "name": "@radiant/ios",
  "version": "0.1.0",
  "private": true,
  "description": "Native iOS shell for Radiant — bundles the UI and connects to a Radiant server on your Mac.",
  "scripts": { "sync": "cap sync ios", "open": "cap open ios" },
  "dependencies": { "@capacitor/core": "^7.0.0", "@capacitor/ios": "^7.0.0" },
  "devDependencies": { "@capacitor/cli": "^7.0.0", "typescript": "^5" }
}
